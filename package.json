{
  "name": "ctr-mode",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Why AES-CTR is unsafe without authentication — interactive demonstrations running real AES in the browser.",
  "scripts": {
    "test": "node --test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.9.0"
  },
  "license": "Apache-2.0"
}
